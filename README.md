# homebrew-tap

Homebrew tap for `fex`.

## Install

```bash
brew install krisfur/tap/fex
```

## Update formula

1. Tag and push a new release in `krisfur/fex`.
2. Update `Formula/fex.rb` with the new `url` and `sha256`.
3. Commit and push this repository.
